# Ledger Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `/ledger`, the occam skill that commits staged changes and writes PR bodies, and make `razor` point at it.

**Architecture:** One new Markdown skill, `skills/ledger/SKILL.md`, holds every rule and table. `razor`'s WRITE section loses its commit and PR paragraphs and gains one pointer line. The invariants test splits so razor keeps the code-comment assertions and ledger gets its own.

**Tech Stack:** Markdown skills, `node --test`, `gh`, `git`. No build.

Spec: `docs/superpowers/specs/2026-09-16-ledger-design.md`.

---

### Task 1: Tests first

**Files:** Modify `tests/skill-invariants.test.js:20-31`.

- [ ] Split the razor WRITE test. Razor keeps `Two lines at most`, `was replaced or removed`, `justifies the decision`; add `ledger`.
- [ ] Add `test('ledger owns the commit and PR-body rules')`: reads `skills/ledger/SKILL.md`; asserts `## What changed`, `## Most impactful`, `## Main flows`, `Co-Authored-By`, `Claude-Session`, `no verification section`, `Nothing staged`.
- [ ] Run `node --test tests/*.test.js`. Expect two failures: ledger file missing, razor lacks `ledger`.

### Task 2: skills/ledger/SKILL.md

**Files:** Create `skills/ledger/SKILL.md`.

- [ ] Frontmatter: `name: ledger`, description with trigger phrases, `argument-hint: "[pr [pr-number]]"`, `allowed-tools: Bash, Read, Glob, Grep, AskUserQuestion`.
- [ ] Sections: Inputs table · Commit · Scope table (copied) · Type table (copied) · Message rules with footer ban · PR: target, conventions, body, sections, title, confirm · Language.
- [ ] Run tests. Ledger test passes.

### Task 3: razor points at ledger

**Files:** Modify `skills/razor/SKILL.md:104-120,182`.

- [ ] Replace the Commit and PR body paragraphs and the example block with one line naming `ledger`.
- [ ] Update `## Boundaries`.
- [ ] Run tests. All pass. `grep -rn tldr skills/ | grep -v razor/SKILL.md` empty.

### Task 4: README and manifests

**Files:** Modify `README.md:18-29`, `README.md` before `## Optional dependencies`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`.

- [ ] Skills table row. "other four" → "other five". `## ledger writes what leaves the session` section.
- [ ] Both descriptions name `ledger`. Bump `1.10.0` → `1.11.0`, three places.
- [ ] Run tests (version agreement).

### Task 5: Memory and hook check

- [ ] Rewrite memory `pr-bodies-bullets-only.md`; drop the footer line. Update `MEMORY.md` hook.
- [ ] Hook smoke test from CLAUDE.md with temp dirs: razor-activate still prints the banner.

### Task 6: Commit

- [ ] One commit per task, conventional, full sentences, no footers. Do not push; the user releases.
